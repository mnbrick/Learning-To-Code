// Use an array in a For Loop to list out its elements in an automated way.
#include <stdio.h>
#include <cs50.h>

int main(void)

{
    char alpha[5] = {'a', 'b', 'z', 'l', 'p'};
    for(int i = 0; i < 5; i++)
    {
     if alpha
    }
}
